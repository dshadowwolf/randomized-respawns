# Randomized Respawns
A simple and quickly coded mod that randomizes the players respawn point if they have not set one

Radius can be `0` to disable the mod or up to `2000` to give a square about 4000 blocks per side (centered at world-spawn) where players might get teleported to on respawn.
